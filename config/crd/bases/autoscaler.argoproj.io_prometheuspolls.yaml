---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: prometheuspolls.autoscaler.argoproj.io
spec:
  group: autoscaler.argoproj.io
  names:
    kind: PrometheusPoll
    listKind: PrometheusPollList
    plural: prometheuspolls
    singular: prometheuspoll
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current status based on Available condition
      jsonPath: .status.conditions[?(@.type=='Available')].status
      name: Available
      type: string
    - description: Current status based on Ready condition
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: Since last poll
      jsonPath: .status.lastPollingTime
      name: Last Polled
      type: date
    - description: Time since creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PrometheusPoll is the Schema for the prometheuspolls API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrometheusPollSpec defines the configuration for this poller.
            properties:
              address:
                description: Address is the address of the Prometheus server.
                type: string
              initialDelay:
                description: InitialDelay is the initial delay before polling after
                  creating a poll or adding a new cluster.
                type: string
              metrics:
                description: Metrics is the list of metrics to poll from Prometheus.
                items:
                  description: Metric is a configuration for polling from Prometheus
                  properties:
                    id:
                      description: ID of this metric.
                      type: string
                    noData:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        NoData if set will be used as the value when no data is available in Prometheus.
                        If not set - missing data will result in error.
                        There are metrics like argocd_app_k8s_request_total that might have a bug and not being reported for prolonged periods of time.
                      format: float
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      type: number
                      x-kubernetes-int-or-string: true
                    query:
                      description: |-
                        Query is the query used to fetch the metric. It is a Go Template with Sprig functions binding.
                        A `.namespace`, `.name` and `.server` are available for the template.
                      type: string
                  required:
                  - id
                  - query
                  type: object
                type: array
              period:
                description: Period is the period of polling.
                type: string
            required:
            - address
            - initialDelay
            - metrics
            - period
            type: object
          status:
            description: PrometheusPollStatus defines the observed state of PrometheusPoll.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastPollingTime:
                description: LastCalculatedTime is the last time the polling was performed
                  for this configuration.
                format: date-time
                type: string
              values:
                description: Values of the metrics polled.
                items:
                  description: MetricValue is a resulting value for the metric.
                  properties:
                    id:
                      description: ID of this metric.
                      type: string
                    ownerRef:
                      description: OwnerRef is the reference to the owner of this
                        metric.
                      properties:
                        apiVersion:
                          description: API version of the referent.
                          type: string
                        fieldPath:
                          description: |-
                            If referring to a piece of an object instead of an entire object, this string
                            should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                            For example, if the object reference is to a container within a pod, this would take on a value like:
                            "spec.containers{name}" (where "name" refers to the name of the container that triggered
                            the event) or if no container name is specified "spec.containers[2]" (container with
                            index 2 in this pod). This syntax is chosen only to have some well-defined way of
                            referencing a part of an object.
                          type: string
                        kind:
                          description: |-
                            Kind of the referent.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                          type: string
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        namespace:
                          description: |-
                            Namespace of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                          type: string
                        resourceVersion:
                          description: |-
                            Specific resourceVersion to which this reference is made, if any.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                          type: string
                        uid:
                          description: |-
                            UID of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    poller:
                      description: Poller is the identification of the poller that
                        was used to fetch this metric.
                      type: string
                    query:
                      description: |-
                        Query is the query that was used to fetch this value.
                        It will be different for individual implementations.
                      type: string
                    value:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Value is the value of the metric.
                      format: float
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      type: number
                      x-kubernetes-int-or-string: true
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
