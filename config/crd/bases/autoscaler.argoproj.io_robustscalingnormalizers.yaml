---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: robustscalingnormalizers.autoscaler.argoproj.io
spec:
  group: autoscaler.argoproj.io
  names:
    kind: RobustScalingNormalizer
    listKind: RobustScalingNormalizerList
    plural: robustscalingnormalizers
    singular: robustscalingnormalizer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current status based on Available condition
      jsonPath: .status.conditions[?(@.type=='Available')].status
      name: Available
      type: string
    - description: Current status based on Ready condition
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: Time since creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RobustScalingNormalizer is the Schema for the robustscalingnormalizers
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RobustScalingNormalizerSpec defines the desired state of
              RobustScalingNormalizer.
            properties:
              pollerRef:
                description: PollerRef is the reference to the poller that we need
                  to normalize.
                properties:
                  apiGroup:
                    description: |-
                      APIGroup is the group for the resource being referenced.
                      If APIGroup is not specified, the specified Kind must be in the core API group.
                      For any other third-party types, APIGroup is required.
                    type: string
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name is the name of resource being referenced
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
            required:
            - pollerRef
            type: object
          status:
            description: RobustScalingNormalizerStatus defines the observed state
              of RobustScalingNormalizer.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              values:
                description: Values of the metrics normalized.
                items:
                  description: MetricValue is a resulting value for the metric.
                  properties:
                    displayValue:
                      description: |-
                        DisplayValue is the string representation of the without precision guarantee.
                        This is meaningless and exists purely for convenience of someone who is looking at the kubectl get output.
                      type: string
                    id:
                      description: ID of this metric.
                      type: string
                    poller:
                      description: Poller is the identification of the poller that
                        was used to fetch this metric.
                      type: string
                    query:
                      description: |-
                        Query is the query that was used to fetch this value.
                        It will be different for individual implementations.
                      type: string
                    shard:
                      description: Shard is this shard for this metric value.
                      properties:
                        data:
                          additionalProperties:
                            type: string
                          description: Data is a map that will be used to populate
                            Go Templates params for the poller (if the poller supports
                            that).
                          type: object
                        id:
                          description: |-
                            ID of this shard. It may or may not be unique, depending on the discoverer.
                            It is expected to be used to populate Go Templates params for the poller (if the poller supports that).
                          type: string
                        uid:
                          description: |-
                            UID unieue identifier of this shard so it can be uniquely identified in the list of shards.
                            The reason this is not baked into SourceRef is because actual resource may or may not exists.
                            When the discovery was external - this may be arbitrary string unique to that shard.
                          type: string
                      required:
                      - data
                      - id
                      - uid
                      type: object
                    value:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Value is the value of the metric.
                      format: float
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      type: number
                      x-kubernetes-int-or-string: true
                  required:
                  - displayValue
                  - id
                  - poller
                  - shard
                  - value
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
